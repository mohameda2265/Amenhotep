import { CompoundFilterPipe } from './compound-filter.pipe';

describe('CompoundFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompoundFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
