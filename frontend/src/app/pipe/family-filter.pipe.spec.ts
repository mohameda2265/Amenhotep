import { FamilyFilterPipe } from './family-filter.pipe';

describe('FamilyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FamilyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
