import { OwnershipsPipe } from './ownerships.pipe';

describe('OwnershipsPipe', () => {
  it('create an instance', () => {
    const pipe = new OwnershipsPipe();
    expect(pipe).toBeTruthy();
  });
});
